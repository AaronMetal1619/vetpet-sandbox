{
  "build": {
    "nixpacks": {
      "version": "v1.16.0"
    },
    "builder": "nixpacks",
    "buildCommand": "composer install --no-dev",
    "startCommand": "php artisan serve --host=0.0.0.0 --port=${PORT}",
    "variables": {
      "PHP_VERSION": "8.2"
    }
  }
}
